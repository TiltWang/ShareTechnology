# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShareTechnology' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for ShareTechnology
#,:inhibit_warnings => true忽略警告
pod 'Masonry',:inhibit_warnings => true
pod 'MJExtension',:inhibit_warnings => true
pod 'FLEX', '~> 2.0', :configurations => ['Debug'],:inhibit_warnings => true
pod 'ChameleonFramework',:inhibit_warnings => true
pod 'SDWebImage',:inhibit_warnings => true
pod 'OAStackView',:inhibit_warnings => true
pod 'AFNetworking',:inhibit_warnings => true
end
